import React from 'react'

class BookNotFound extends React.Component {
    render() {
        return(<div style={{textAlign: "center"}}>Book not found, try another title!</div>)
    }
}

export default BookNotFound